/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keep the default Next.js routing behavior so / renders the app page rather than the legacy static HTML.
};

export default nextConfig;
